namespace OrderHub.Tests.Domain;

public class PropertyTests
{
    // Property Should Throw exception if name is empty
    // Property Should Throw exception if type is list and has no options
    // Property should throw's exception if duplicate options
}
