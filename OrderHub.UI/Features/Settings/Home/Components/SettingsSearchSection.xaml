<UserControl
    x:Class="OrderHub.UI.Features.Settings.Home.Components.SettingsSearchSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Margin="0,24,0,24"
        Padding="24"
        Background="White"
        BorderBrush="#E5E7EB"
        BorderThickness="1"
        CornerRadius="12">

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Left  -->

            <StackPanel>

                <TextBlock
                    FontSize="18"
                    FontWeight="SemiBold"
                    Foreground="#1F2937"
                    Text="ابحث عن إعداد" />

                <TextBlock
                    Margin="0,4,0,20"
                    FontSize="13"
                    Foreground="#6B7280"
                    Text="اكتب اسم الإعداد أو الوحدة للوصول إليها بسرعة." />

                <hc:SearchBar
                    Width="420"
                    Height="42"
                    HorizontalAlignment="Left"
                    hc:InfoElement.Placeholder="مثال: الخصائص، المدن، الأقسام..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            </StackPanel>

            <!--  Right  -->

            <Border
                Grid.Column="1"
                Width="70"
                Height="70"
                Margin="24,0,0,0"
                VerticalAlignment="Center"
                Background="#EEF4FF"
                CornerRadius="16">

                <icons:MaterialIcon
                    Width="34"
                    Height="34"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="#2563EB"
                    Kind="Magnify" />

            </Border>

        </Grid>

    </Border>

</UserControl>