<UserControl
    x:Class="OrderHub.UI.Features.Orders.Index.OrderSummaryPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Width="250"
        Padding="12"
        Background="#f8fafc"
        BorderBrush="#e2e8f0"
        BorderThickness="1"
        CornerRadius="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontSize="16"
                    FontWeight="DemiBold"
                    Text="ملخص الطلبات" />

                <TextBlock
                    Margin="0,0,0,6"
                    FontSize="14"
                    FontWeight="DemiBold"
                    Text="الحالات" />
                <ItemsControl ItemsSource="{Binding StatusSummaries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,0,6"
                                Padding="10,8"
                                Background="{StaticResource LightBlueBrush}"
                                CornerRadius="8">
                                <TextBlock
                                    FontWeight="DemiBold"
                                    Text="{Binding MenuText}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock
                    Margin="0,10,0,6"
                    FontSize="14"
                    FontWeight="DemiBold"
                    Text="وسائل الدفع" />
                <ItemsControl ItemsSource="{Binding PaymentMethodSummaries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,0,6"
                                Padding="10,8"
                                Background="{StaticResource LightBlueBrush}"
                                CornerRadius="8">
                                <TextBlock
                                    FontWeight="DemiBold"
                                    Text="{Binding MenuText}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
