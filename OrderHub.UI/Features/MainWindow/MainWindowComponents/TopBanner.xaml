<UserControl
    x:Class="OrderHub.UI.Features.MainWindow.MainWindowComponents.TopBanner"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
    xmlns:local="clr-namespace:OrderHub.UI.Features.MainWindow.MainWindowComponents"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Padding="8,6"
        Background="White"
        BorderBrush="#E5E7EB"
        BorderThickness="1"
        CornerRadius="6">

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Current Page  -->

            <TextBlock
                VerticalAlignment="Center"
                FontSize="15"
                FontWeight="SemiBold"
                Foreground="#374151"
                Text="{Binding NavigationCommand.Name}" />

            <!--  Right Section  -->

            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal">

                <Button
                    Width="30"
                    Height="30"
                    Margin="0,0,8,0"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand">

                    <icons:MaterialIcon
                        Width="18"
                        Height="18"
                        Foreground="{StaticResource DangerBrush}"
                        Kind="NotificationsNone" />

                </Button>

                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="12"
                    FontWeight="Medium"
                    Foreground="#4B5563"
                    Text="Username" />

            </StackPanel>

        </Grid>

    </Border>

</UserControl>