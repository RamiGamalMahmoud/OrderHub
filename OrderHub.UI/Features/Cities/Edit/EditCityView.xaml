<UserControl
    x:Class="OrderHub.UI.Features.Cities.Edit.EditCityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:OrderHub.UI.Features.Cities.Edit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:ViewModel}"
    FontFamily="{StaticResource FontFamily}"
    mc:Ignorable="d">

    <Border
        MinWidth="360"
        Padding="28"
        Background="White">

        <Grid Width="320" FlowDirection="RightToLeft">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Field Label  -->
            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,8"
                FontSize="14"
                FontWeight="SemiBold"
                Text="اسم المدينة" />

            <!--  Field  -->
            <TextBox
                Grid.Row="1"
                MinHeight="40"
                Padding="10,6"
                FontSize="14"
                Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <!--  Actions  -->
            <StackPanel
                Grid.Row="2"
                Margin="0,24,0,0"
                HorizontalAlignment="Center"
                Orientation="Horizontal">

                <Button
                    MinWidth="100"
                    MinHeight="40"
                    Margin="0,0,8,0"
                    Command="{Binding SaveCommand}"
                    Content="حفظ"
                    Cursor="Hand"
                    IsDefault="True"
                    Style="{StaticResource ButtonPrimary}" />

                <Button
                    MinWidth="100"
                    MinHeight="40"
                    Command="hc:ControlCommands.Close"
                    Content="إلغاء"
                    Cursor="Hand" />

            </StackPanel>

        </Grid>
    </Border>

</UserControl>